# ========== APPLICATION.YML - Auth Service ==========

spring:
  application:
    name: auth-service
  
  datasource:
    url: postgresql://postgres.wbtphqctdvyejjtgucuk:123456@aws-0-us-west-2.pooler.supabase.com:5432/postgres
    username: ${DB_USER:ganasoganas23@gmail.com}
    password: ${DB_PASSWORD=123456}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
  
  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: ${PORT:8081}
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.siempregana: DEBUG

jwt:
  secret: ${YXZRvorlVAlZNCr0URrQCmgzGq/zLMXZn7wUuM+Cg1g=}
  expiration: ${JWT_EXPIRATION_ACCESS_TOKEN=36000000}
  refresh-expiration: ${JWT_EXPIRATION_REFRESH_TOKEN=60480000}